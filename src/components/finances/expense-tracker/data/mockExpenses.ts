
// This file previously contained mock expenses - now completely empty as we use real Supabase data
// All expense data is now fetched from the database through the useExpenseTracker hook
// No synthetic or mock data should be used for authenticated users

console.log("Mock expenses file - all data should come from Supabase database");

export {};
